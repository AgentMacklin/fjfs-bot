{
  "name": "fjfs-bot",
  "version": "1.0.0",
  "description": "A family-friendly discord bot",
  "main": "src/index.js",
  "scripts": {
    "build": "tsc --build",
    "start": "tsc --build && node build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "discord.js": "^12.3.1"
  },
  "devDependencies": {
    "@types/node": "^14.11.5",
    "@types/ws": "^7.2.7",
    "typescript": "^4.0.3"
  }
}
